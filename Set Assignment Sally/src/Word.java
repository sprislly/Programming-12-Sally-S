java.util.ArrayList;

public class Word {
    //if str contains ' s , X x
    String word;

    Word(String word){
        this.word;
    }
    // removes single letter words.
    if((word.length == 1) && (word.equals("a")) || (word.equals ("i")) {
        isWord = false;
    }

    //array list of roman numerals
    ArrayList<Word> romanNumerals = new ArrayList<>();

    //removes roman numerals by checking if the value is the same


}
