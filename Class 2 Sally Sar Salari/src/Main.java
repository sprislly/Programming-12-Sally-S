import java.util.ArrayList;

public class Main {
    Person person = new Person(name "Jimbo");
    Person person2 = new Person(name "Jimbo");
    System.out.println(person); //cant == string, different
            //prim data type aray also object
    //reference data = person>add>data
    //make fields mostly private unless static
    //methods public == private fields, public methods


    //array, reg array, ref == array list
    int[] numbers = new int[10];
    ArrayList<Person> people = new ArrayList<>();
    people.add(person);

}
